import {TIMEOUT} from "../constants/config";

const config = {
    targetWindow: null,
    targetEndpoint: null,
    sourceIdentifier: null,
    targetIdentifier: null,
    timeout: TIMEOUT,
};

export default config;
